% \begin{figure}[t]
% \includegraphics[width=\textwidth]{images/sw_example_ninon.png}
% \centering
% \caption{Example bioinformatics workflow for differential gene expression analysis created by one domain expert recruited in our study. The figure highlights the conceptional schema of the workflow (left), it's implementation in nextflow (middle) as well as the implementation of one single step (right), i.e. reference genome alignment using the STAR tool. The workflow comprises seven computational steps in total. For each step the used tool is given in blue below the task description.}
% \label{fig:sw_example}
% \end{figure}


\begin{figure*}
    \centering
    \subfloat[][]{
        \includegraphics[height=0.5\textwidth]{images/sw_ninon_a.png}
        \label{fig:ninon_a}}
    \subfloat[][]{
        \includegraphics[height=0.5\textwidth]{images/sw_ninon_b.png}
        \label{fig:ninon_b}}
    \subfloat[][]{
        \includegraphics[height=0.5\textwidth]{images/sw_ninon_c.png}
        \label{fig:ninon_c}}
    \caption{Example bioinformatics workflow for differential gene expression analysis created by one domain expert recruited in our study. The figure highlights the conceptional schema of the workflow (a), its implementation in Nextflow (b), and the implementation of one single step (c), i.e., reference genome alignment using the STAR tool. The workflow comprises six computational steps in total. For each step the used tool is given in blue below the task description.}
    \label{fig:ninon}
\end{figure*}